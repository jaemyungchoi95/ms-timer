# ms-timer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 18:00:00.000까지 남은 시간을 시:분:초 플립 카드 + ms 스크롤 릴로 표시하는 Windows 데스크톱 앱(portable exe)을 만든다.

**Architecture:** 로직은 `src/lib/`의 순수 함수 2개(`computeRemaining`, `reelPhase`)에 전부 격리되어 node:test로 검증된다. `src/renderer/`는 로직 없는 표현 계층이며 테스트하지 않는다. Electron main은 BrowserWindow 생성만 한다. rAF 루프 하나가 매 프레임 절대 시각을 재계산해 카드 6개 + 릴 3개를 갱신한다. IPC·preload는 없다.

**Tech Stack:** Electron 43, electron-builder 26, node:test (내장), 순수 ESM (번들러 없음, 런타임 의존성 0)

**Spec:** `docs/superpowers/specs/2026-07-15-ms-timer-design.md`

## Global Constraints

- **프로젝트 경로는 `/mnt/c/Users/cloocus/ms-timer` 고정.** WSL 파일시스템(`/home/...`)으로 옮기면 Windows node가 `\\wsl$` UNC 경로로 접근하며 electron-builder가 실패한다.
- **`npm install` / electron 실행 / 빌드는 반드시 Windows node로** (`cmd.exe /c "..."`). WSL npm으로 install하면 electron 바이너리가 linux용으로 받아진다.
- **테스트는 WSL node로 실행** (`node --test`). 순수 JS라 의존성이 없어 interop 오버헤드 없이 빠르다.
- 런타임 의존성 0. `dependencies`는 비운다. electron / electron-builder는 `devDependencies`.
- 모든 소스는 ESM (`"type": "module"`). 번들러·트랜스파일러 없음.
- Electron 보안 기본값 유지: `nodeIntegration: false`, `contextIsolation: true`. CSP meta 태그는 넣지 않는다(spec의 "보안" 절 참조).
- 시간 계산은 **절대 시각 차분**만 사용. 누적 감산(`remaining -= 16`) 금지.
- `src/lib/`는 DOM을 참조하지 않는다. `src/renderer/`는 로직을 담지 않는다. 테스트는 `src/lib/`에만 존재한다.
- 애니메이션은 `transform`만 사용한다(GPU 합성). `top`/`left`/`height` 애니메이션 금지.
- 커밋 메시지는 Conventional Commits (`feat:`, `test:`, `chore:`, `docs:`).

---

### Task 1: 순수 함수 2개 + 프로젝트 스캐폴딩

이 앱의 로직 전부. Electron 없이 완결되며 WSL node로 즉시 테스트된다.

**Files:**
- Create: `/mnt/c/Users/cloocus/ms-timer/package.json`
- Create: `/mnt/c/Users/cloocus/ms-timer/src/lib/countdown.js`
- Create: `/mnt/c/Users/cloocus/ms-timer/src/lib/reel-phase.js`
- Test: `/mnt/c/Users/cloocus/ms-timer/test/countdown.test.js`
- Test: `/mnt/c/Users/cloocus/ms-timer/test/reel-phase.test.js`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces:
  - `computeRemaining(now: Date, targetHour: number = 18) → { expired: boolean, h: number, m: number, s: number, ms: number }` — `expired`가 `true`면 나머지 필드는 전부 `0`. 입력 `now`를 변경하지 않는다.
  - `reelPhase(ms: number, place: number) → number` — `0` 이상 `10` 미만. `place`는 `100` | `10` | `1`.
  - Task 2 이후의 `src/renderer/clock.js`가 `computeRemaining`을, `src/renderer/reel.js`가 `reelPhase`를 import한다.

- [ ] **Step 1: package.json 생성**

`author`는 electron-builder가 요구한다(없으면 빌드 실패). `dependencies`는 의도적으로 없다.

```json
{
  "name": "ms-timer",
  "version": "1.0.0",
  "description": "18시 정각까지 1ms 단위 카운트다운 플립 시계",
  "author": "jmchoi",
  "license": "MIT",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "start": "electron .",
    "build": "electron-builder --win portable"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.0"
  },
  "build": {
    "appId": "com.jmchoi.ms-timer",
    "productName": "ms-timer",
    "files": ["src/**/*", "package.json"],
    "win": {
      "target": "portable"
    },
    "portable": {
      "artifactName": "ms-timer.exe"
    }
  }
}
```

- [ ] **Step 2: countdown 실패 테스트 작성**

`at()` 헬퍼는 **로컬 시각** Date를 만든다(월은 0-indexed이므로 `6` = 7월). `computeRemaining`의 `setHours`도 로컬 기준이라 타임존과 무관하게 일관된다.

마지막 두 테스트가 중요하다: `targetHour` 파라미터화는 spec의 "확장 가능" 주장을 보장하고, 순수성 테스트는 `now.setHours()`로 입력을 오염시키는 실수를 잡는다.

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { computeRemaining } from '../src/lib/countdown.js';

const at = (h, m, s, ms) => new Date(2026, 6, 15, h, m, s, ms);

test('정상 카운트다운 — 09:00이면 9시간 남음', () => {
  assert.deepEqual(computeRemaining(at(9, 0, 0, 0)),
    { expired: false, h: 9, m: 0, s: 0, ms: 0 });
});

test('18시 1ms 전', () => {
  assert.deepEqual(computeRemaining(at(17, 59, 59, 999)),
    { expired: false, h: 0, m: 0, s: 0, ms: 1 });
});

test('18시 정각 — expired', () => {
  assert.deepEqual(computeRemaining(at(18, 0, 0, 0)),
    { expired: true, h: 0, m: 0, s: 0, ms: 0 });
});

test('18시 1ms 후 — expired', () => {
  assert.deepEqual(computeRemaining(at(18, 0, 0, 1)),
    { expired: true, h: 0, m: 0, s: 0, ms: 0 });
});

test('18시 이후 실행 — 즉시 expired', () => {
  assert.deepEqual(computeRemaining(at(19, 30, 0, 0)),
    { expired: true, h: 0, m: 0, s: 0, ms: 0 });
});

test('자정 직후 — 같은 날 18시까지 18시간 (자정 롤오버)', () => {
  assert.deepEqual(computeRemaining(at(0, 0, 0, 0)),
    { expired: false, h: 18, m: 0, s: 0, ms: 0 });
});

test('자릿수 혼합 — 08:07:06.005', () => {
  assert.deepEqual(computeRemaining(at(8, 7, 6, 5)),
    { expired: false, h: 9, m: 52, s: 53, ms: 995 });
});

test('targetHour 인자로 목표 시각 변경', () => {
  assert.deepEqual(computeRemaining(at(17, 0, 0, 0), 19),
    { expired: false, h: 2, m: 0, s: 0, ms: 0 });
});

test('입력 Date를 변경하지 않는다 (순수 함수)', () => {
  const now = at(9, 0, 0, 0);
  const before = now.getTime();
  computeRemaining(now);
  assert.equal(now.getTime(), before);
});
```

- [ ] **Step 3: reel-phase 실패 테스트 작성**

마지막 테스트가 릴의 계약을 지킨다 — 위상이 `[0, 10)`을 벗어나면 릴 스트립이 창 밖으로 나가 빈칸이 보인다.

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { reelPhase } from '../src/lib/reel-phase.js';

test('백의자리 — 시작', () => {
  assert.equal(reelPhase(0, 100), 0);
});

test('백의자리 — 중간', () => {
  assert.equal(reelPhase(450, 100), 4.5);
});

test('백의자리 — 최대', () => {
  assert.equal(reelPhase(999, 100), 9.99);
});

test('십의자리 — 백의자리를 무시하고 wrap', () => {
  assert.equal(reelPhase(450, 10), 5);
});

test('십의자리 — 최대', () => {
  assert.equal(reelPhase(999, 10), 9.9);
});

test('일의자리 — 최대', () => {
  assert.equal(reelPhase(999, 1), 9);
});

test('일의자리 — 450ms는 일의자리가 0', () => {
  assert.equal(reelPhase(450, 1), 0);
});

test('0..999 전 구간에서 위상은 항상 [0, 10)', () => {
  for (let ms = 0; ms < 1000; ms++) {
    for (const place of [100, 10, 1]) {
      const phase = reelPhase(ms, place);
      assert.ok(phase >= 0 && phase < 10,
        `reelPhase(${ms}, ${place}) = ${phase} — [0,10) 범위 이탈`);
    }
  }
});
```

- [ ] **Step 4: 테스트 실패 확인**

```bash
cd /mnt/c/Users/cloocus/ms-timer && node --test
```

Expected: FAIL — `Cannot find module '.../src/lib/countdown.js'` 및 `.../src/lib/reel-phase.js`

- [ ] **Step 5: countdown.js 구현**

`new Date(now)`는 복사 생성자다 — 입력을 오염시키지 않는다. `now.setHours(...)`를 직접 호출하면 순수성 테스트가 깨진다.

```js
/**
 * 같은 날짜의 목표 시각까지 남은 시간을 계산한다.
 *
 * 절대 시각 차분 방식이므로 드리프트가 누적되지 않고,
 * 절전 복귀 / NTP 보정 / 타임존 변경이 자동으로 반영된다.
 * 매 호출마다 now의 날짜로 목표를 재계산하므로 자정 롤오버가 공짜다.
 *
 * @param {Date} now 현재 시각 (변경되지 않음)
 * @param {number} targetHour 목표 시(로컬), 기본 18
 * @returns {{expired: boolean, h: number, m: number, s: number, ms: number}}
 */
export function computeRemaining(now, targetHour = 18) {
  const target = new Date(now);
  target.setHours(targetHour, 0, 0, 0);

  const diff = target.getTime() - now.getTime();

  if (diff <= 0) {
    return { expired: true, h: 0, m: 0, s: 0, ms: 0 };
  }

  return {
    expired: false,
    h: Math.floor(diff / 3600000),
    m: Math.floor(diff / 60000) % 60,
    s: Math.floor(diff / 1000) % 60,
    ms: diff % 1000,
  };
}
```

- [ ] **Step 6: reel-phase.js 구현**

```js
/**
 * ms 값에서 해당 자릿수 릴의 연속 위상을 구한다.
 *
 * 이산적인 자릿값(floor)이 아니라 연속값을 반환하는 것이 요점이다.
 * 릴이 프레임 사이에도 계속 흐르므로 60Hz 샘플링에서 잔상이 생긴다.
 *
 * place=100 → 초당 1바퀴, place=10 → 10바퀴, place=1 → 100바퀴.
 *
 * @param {number} ms 0..999
 * @param {number} place 100 | 10 | 1
 * @returns {number} 0 이상 10 미만
 */
export function reelPhase(ms, place) {
  return (ms % (place * 10)) / place;
}
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
cd /mnt/c/Users/cloocus/ms-timer && node --test
```

Expected: PASS — `# pass 17`, `# fail 0`

- [ ] **Step 8: 커밋**

```bash
cd /mnt/c/Users/cloocus/ms-timer
git add package.json src/lib/ test/
git commit -m "feat: 카운트다운/릴위상 순수 함수

computeRemaining — 절대 시각 차분. 드리프트/절전복귀/NTP보정 자동 대응.
자정 롤오버는 매 호출 시 now 날짜 기준 재계산으로 처리.
reelPhase — 이산 자릿값이 아닌 연속 위상. 60Hz에서 잔상이 생기는 근거."
```

---

### Task 2: Electron 셸 + 플립 카드 레이아웃 (정적)

창이 뜨고 카드 6개가 값을 표시하는 데까지. **애니메이션은 다음 태스크다** — 여기서는 값이 바뀌면 즉시 교체된다.

**Files:**
- Create: `/mnt/c/Users/cloocus/ms-timer/src/main.js`
- Create: `/mnt/c/Users/cloocus/ms-timer/src/renderer/index.html`
- Create: `/mnt/c/Users/cloocus/ms-timer/src/renderer/themes.css`
- Create: `/mnt/c/Users/cloocus/ms-timer/src/renderer/style.css`
- Create: `/mnt/c/Users/cloocus/ms-timer/src/renderer/flip-digit.js`
- Create: `/mnt/c/Users/cloocus/ms-timer/src/renderer/clock.js`

**Interfaces:**
- Consumes: `computeRemaining(now, targetHour)` from `src/lib/countdown.js` (Task 1)
- Produces:
  - `FlipDigit` 클래스 — 생성자 `new FlipDigit(rootEl)`, 메서드 `set(char: string)`. Task 3이 이 클래스에 플립 애니메이션을 추가한다.
  - `src/renderer/style.css`의 CSS 변수 `--card-w` / `--card-h` / `--card-bg` 등 — Task 3(플립 조각)과 Task 4(릴 크기)가 이 값들에 의존한다.
  - `index.html`의 `[data-flip]` 요소 6개와 `#clock` / `#label` — Task 4가 `[data-reel]`을, Task 5가 `:root[data-theme]`을 추가한다.
  - `src/main.js` — `package.json`의 `main` 진입점. Task 6의 빌드가 사용한다.

- [ ] **Step 1: Windows node로 의존성 설치**

electron 바이너리(~100MB+)를 받으므로 1-3분 걸린다. **반드시 `cmd.exe /c`로 실행.**

```bash
cd /mnt/c/Users/cloocus/ms-timer && cmd.exe /c "npm install"
```

Expected: `added N packages`

- [ ] **Step 2: 설치 검증 — linux 바이너리를 받지 않았는지 확인**

이 검증을 건너뛰면 Step 9에서 원인 불명으로 실패한다.

**주의 — electron 43은 `npm install` 직후에 바이너리가 없다.** 이 버전은 `postinstall` 스크립트가 없고(`node_modules/electron/package.json`의 `scripts`가 비어 있다) 첫 실행 시점에 lazy download한다. 따라서 install 직후 `electron.exe`를 찾으면 **정상 상태에서도 실패한다**.

검증은 파일 존재가 아니라 **바이너리의 정체**로 한다. Step 9에서 앱을 처음 띄운 뒤 실행한다:

```bash
file /mnt/c/Users/cloocus/ms-timer/node_modules/electron/dist/electron.exe
```

Expected: `PE32+ executable (GUI) x86-64, for MS Windows` — Windows 네이티브 바이너리.
FAIL 시(`ELF 64-bit ... GNU/Linux` = WSL npm으로 설치된 것):
```bash
cd /mnt/c/Users/cloocus/ms-timer && rm -rf node_modules package-lock.json && cmd.exe /c "npm install"
```

- [ ] **Step 3: main.js 작성**

`import.meta.dirname`은 ESM에 `__dirname`이 없어서 필요하다(Electron 43은 Node 22 번들 — 지원됨).
`backgroundThrottling: false`가 핵심이다 — 없으면 창이 가려질 때 Chromium이 rAF를 멈춘다.
`backgroundColor`는 로드 시 흰 화면 깜빡임을 막는다.

```js
import { app, BrowserWindow } from 'electron';
import path from 'node:path';

function createWindow() {
  const win = new BrowserWindow({
    width: 720,
    height: 320,
    minWidth: 420,
    minHeight: 200,
    title: '퇴근까지',
    backgroundColor: '#0b0d10',
    webPreferences: {
      backgroundThrottling: false,
      nodeIntegration: false,
      contextIsolation: true,
    },
  });

  win.removeMenu();
  win.loadFile(path.join(import.meta.dirname, 'renderer/index.html'));
}

app.whenReady().then(createWindow);

app.on('window-all-closed', () => {
  app.quit();
});
```

- [ ] **Step 4: index.html 작성**

카드 6개는 `data-flip` 속성으로 식별하며 순서가 곧 `HHMMSS`다. 각 카드는 상/하 두 조각으로 나뉘고, 조각 안의 `<span>`은 카드 전체 높이를 가져 숫자가 카드 중앙에 놓인다 — 조각의 `overflow: hidden`이 위/아래 절반만 노출한다. Task 3이 여기에 접히는 조각 2개를 더한다.

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <title>퇴근까지</title>
    <link rel="stylesheet" href="themes.css" />
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <main id="clock" class="clock">
      <div class="frame">
        <div class="flip" data-flip>
          <div class="flip-top"><span>0</span></div>
          <div class="flip-bottom"><span>0</span></div>
        </div>
        <div class="flip" data-flip>
          <div class="flip-top"><span>0</span></div>
          <div class="flip-bottom"><span>0</span></div>
        </div>

        <div class="sep">:</div>

        <div class="flip" data-flip>
          <div class="flip-top"><span>0</span></div>
          <div class="flip-bottom"><span>0</span></div>
        </div>
        <div class="flip" data-flip>
          <div class="flip-top"><span>0</span></div>
          <div class="flip-bottom"><span>0</span></div>
        </div>

        <div class="sep">:</div>

        <div class="flip" data-flip>
          <div class="flip-top"><span>0</span></div>
          <div class="flip-bottom"><span>0</span></div>
        </div>
        <div class="flip" data-flip>
          <div class="flip-top"><span>0</span></div>
          <div class="flip-bottom"><span>0</span></div>
        </div>
      </div>
      <div id="label" class="label">퇴근까지</div>
    </main>
    <script type="module" src="clock.js"></script>
  </body>
</html>
```

- [ ] **Step 5: themes.css 작성**

팔레트만 담는다. 구조는 `style.css`가 담당하며 서로 침범하지 않는다. Task 5가 `[data-theme="retro"]`를 실제로 활성화하지만, 변수는 지금 정의해둔다 — 두 파일을 오가며 고치는 것보다 낫다.

```css
:root,
:root[data-theme="dark"] {
  --bg: #0b0d10;
  --frame-bg: #15181d;
  --card-bg: #1c1f24;
  --card-fg: #f7f8f8;
  --card-divider: #0b0d10;
  --card-shadow: 0 2px 6px rgba(0, 0, 0, 0.5);
  --sep-fg: #3a3f46;
  --reel-fg: #6b7280;
  --label-fg: #6b7280;
  --accent: #4ade80;
}

:root[data-theme="retro"] {
  --bg: #8fc4b2;
  --frame-bg: #4f8f7c;
  --card-bg: #f4f0e4;
  --card-fg: #2c4a3e;
  --card-divider: #cbc5b4;
  --card-shadow: 0 2px 6px rgba(0, 0, 0, 0.25);
  --sep-fg: #2c4a3e;
  --reel-fg: #6d8a7d;
  --label-fg: #2c4a3e;
  --accent: #1f6b4a;
}
```

- [ ] **Step 6: style.css 작성**

`--card-h`가 모든 치수의 기준이다 — 카드 폭·글자 크기·모서리·릴이 전부 여기서 파생되므로 창 크기가 바뀌어도 비율이 유지된다.

`.flip-top span`과 `.flip-bottom span`이 핵심 기법이다: span은 카드 **전체** 높이(`--card-h`)와 같은 `line-height`를 가져 숫자가 카드 중앙에 놓인다. 조각은 높이가 절반이고 `overflow: hidden`이므로, 상단 조각은 숫자의 위 절반만, 하단 조각은 span을 위로 당겨(`margin-top: -50%`) 아래 절반만 노출한다.

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --card-h: clamp(3rem, min(11vw, 30vh), 7.5rem);
  --card-w: calc(var(--card-h) * 0.68);
  --card-radius: calc(var(--card-h) * 0.08);
  --digit-size: calc(var(--card-h) * 0.7);
  --gap: calc(var(--card-h) * 0.06);
}

body {
  height: 100vh;
  overflow: hidden;
  background: var(--bg);
  font-family: "Segoe UI", system-ui, sans-serif;
  user-select: none;
  -webkit-user-select: none;
  cursor: default;
}

.clock {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: calc(var(--card-h) * 0.18);
}

.frame {
  display: flex;
  align-items: center;
  gap: var(--gap);
  padding: calc(var(--card-h) * 0.16);
  border-radius: calc(var(--card-radius) * 1.8);
  background: var(--frame-bg);
}

.sep {
  font-size: calc(var(--digit-size) * 0.5);
  font-weight: 600;
  color: var(--sep-fg);
  padding: 0 calc(var(--gap) * 0.5);
}

/* 카드 */
.flip {
  position: relative;
  width: var(--card-w);
  height: var(--card-h);
  border-radius: var(--card-radius);
  box-shadow: var(--card-shadow);
  perspective: calc(var(--card-h) * 4);
}

.flip > div {
  position: absolute;
  left: 0;
  width: 100%;
  height: 50%;
  overflow: hidden;
  background: var(--card-bg);
}

.flip > div > span {
  display: block;
  height: var(--card-h);
  line-height: var(--card-h);
  text-align: center;
  font-size: var(--digit-size);
  font-weight: 600;
  color: var(--card-fg);
}

.flip-top {
  top: 0;
  border-radius: var(--card-radius) var(--card-radius) 0 0;
  border-bottom: 1px solid var(--card-divider);
}

.flip-bottom {
  bottom: 0;
  border-radius: 0 0 var(--card-radius) var(--card-radius);
}

/* 하단 조각: span을 위로 당겨 숫자의 아래 절반만 노출 */
.flip-bottom > span {
  margin-top: calc(var(--card-h) / -2);
}

.label {
  font-size: calc(var(--card-h) * 0.15);
  letter-spacing: 0.35em;
  color: var(--label-fg);
}

.clock.expired .flip > div > span {
  color: var(--accent);
}

.clock.expired .label {
  color: var(--accent);
}
```

- [ ] **Step 7: flip-digit.js 작성 (정적 버전)**

`set()`이 값 비교로 조기 반환하는 것이 Task 3의 토대다 — 플립을 "값이 실제로 바뀐 프레임"에만 트리거하기 위해서다. 매 프레임 무조건 갱신하면 Task 3에서 애니메이션이 영원히 재시작되어 카드가 넘어가지 않는다.

```js
/**
 * 플립 시계의 카드 한 장. 자기 값이 바뀔 때만 DOM을 건드린다.
 */
export class FlipDigit {
  /** @param {HTMLElement} root `[data-flip]` 요소 */
  constructor(root) {
    this.root = root;
    this.topSpan = root.querySelector('.flip-top > span');
    this.bottomSpan = root.querySelector('.flip-bottom > span');
    this.value = null;
  }

  /** @param {string} next 한 자리 숫자 문자 */
  set(next) {
    if (next === this.value) return;
    this.value = next;
    this.topSpan.textContent = next;
    this.bottomSpan.textContent = next;
  }
}
```

- [ ] **Step 8: clock.js 작성**

`lastExpired` 캐싱은 마이크로 최적화가 아니다 — 없으면 `classList.toggle`과 label 쓰기가 초당 60회 무의미하게 반복된다. 카드는 `FlipDigit.set()`이 이미 값 비교를 하므로 여기서 또 비교하지 않는다.

```js
import { computeRemaining } from '../lib/countdown.js';
import { FlipDigit } from './flip-digit.js';

const clockEl = document.getElementById('clock');
const labelEl = document.getElementById('label');
const digits = [...document.querySelectorAll('[data-flip]')].map((el) => new FlipDigit(el));

const pad2 = (n) => String(n).padStart(2, '0');

let lastExpired = null;

function tick() {
  const { expired, h, m, s } = computeRemaining(new Date());

  const chars = pad2(h) + pad2(m) + pad2(s);
  for (let i = 0; i < digits.length; i++) {
    digits[i].set(chars[i]);
  }

  if (expired !== lastExpired) {
    clockEl.classList.toggle('expired', expired);
    labelEl.textContent = expired ? '퇴근' : '퇴근까지';
    lastExpired = expired;
  }

  requestAnimationFrame(tick);
}

requestAnimationFrame(tick);
```

- [ ] **Step 9: 실행 및 육안 검증**

```bash
cd /mnt/c/Users/cloocus/ms-timer && cmd.exe /c "npx electron ."
```

Expected — 다음을 **전부** 확인한다:
1. 창이 뜨고 카드 6장에 `HH:MM:SS`가 표시된다 (18시까지 남은 시간)
2. **각 카드의 숫자가 위아래로 잘리지 않고 온전히 보인다** — 잘리면 `.flip-bottom > span`의 `margin-top` 문제다
3. 카드 중앙에 가로 divider 선이 보인다
4. 초의 일의자리가 1초마다 바뀐다 (애니메이션 없이 즉시 교체 — 정상)
5. 창을 리사이즈하면 카드가 비율을 유지한 채 스케일된다
6. 창을 닫으면 프로세스가 종료된다

- [ ] **Step 10: 커밋**

```bash
cd /mnt/c/Users/cloocus/ms-timer
git add package-lock.json src/main.js src/renderer/
git commit -m "feat: Electron 셸 + 플립 카드 레이아웃

카드 상/하 조각 + overflow hidden으로 숫자 반쪽 노출.
FlipDigit.set()은 값 변경 시에만 DOM 조작 — 플립 애니메이션의 토대.
backgroundThrottling:false — 창이 가려져도 rAF 유지."
```

---

### Task 3: 플립 애니메이션

카드가 실제로 넘어간다. 이 태스크가 사용자가 요청한 그래픽의 핵심이다.

**Files:**
- Modify: `/mnt/c/Users/cloocus/ms-timer/src/renderer/index.html` (카드마다 접히는 조각 2개 추가)
- Modify: `/mnt/c/Users/cloocus/ms-timer/src/renderer/style.css` (플립 조각 + keyframes 추가)
- Modify: `/mnt/c/Users/cloocus/ms-timer/src/renderer/flip-digit.js` (플립 트리거)

**Interfaces:**
- Consumes: `FlipDigit` 클래스와 `.flip` / `.flip-top` / `.flip-bottom` CSS (Task 2)
- Produces: 변경 없음 — `FlipDigit.set(char)` 시그니처가 그대로다. Task 4·5는 이 태스크를 몰라도 된다.

**플립 물리:** 실제 split-flap과 같다. 이전 값의 상단 절반이 아래로 접히며(0~150ms) 그 뒤에 있던 새 값의 상단이 드러나고, 이어서 새 값의 하단 절반이 위에서 펼쳐져(150~300ms) 이전 값의 하단을 덮는다.

레이어 역할:
- `.flip-top` — 새 값 상단. 접히는 카드 뒤에 숨어 있다가 드러난다
- `.flip-bottom` — **이전 값** 하단. 펼쳐지는 카드가 덮을 때까지 남아 있다
- `.flip-fold-top` — 이전 값 상단. `rotateX(0 → -90deg)`, origin bottom
- `.flip-fold-bottom` — 새 값 하단. `rotateX(90 → 0deg)`, origin top

- [ ] **Step 1: index.html — 카드마다 접히는 조각 2개 추가**

`.flip` 6개 **전부**를 아래 구조로 바꾼다. 조각 순서가 곧 z-order 기본값이므로 fold 조각이 뒤에 와야 한다.

```html
<div class="flip" data-flip>
  <div class="flip-top"><span>0</span></div>
  <div class="flip-bottom"><span>0</span></div>
  <div class="flip-fold-top"><span>0</span></div>
  <div class="flip-fold-bottom"><span>0</span></div>
</div>
```

- [ ] **Step 2: style.css — 플립 조각 스타일 + keyframes 추가**

파일 끝의 `.label` 규칙 **앞에** 삽입한다.

`display: none` → `block` 전환이 중요하다: 정지 상태에서 fold 조각이 보이면 이전 값이 카드를 덮은 채로 남는다.
`animation-fill-mode: both`가 fold-bottom의 150ms 지연 동안 시작 상태(`rotateX(90deg)`, 모서리만 보임)를 유지시킨다. 없으면 지연 중에 새 값 하단이 그대로 보여 플립이 무너진다.

```css
/* 접히는 조각 — 정지 상태에선 존재하지 않는다 */
.flip-fold-top,
.flip-fold-bottom {
  display: none;
  z-index: 2;
}

.flip-fold-top {
  top: 0;
  border-radius: var(--card-radius) var(--card-radius) 0 0;
  border-bottom: 1px solid var(--card-divider);
  transform-origin: bottom;
}

.flip-fold-bottom {
  bottom: 0;
  border-radius: 0 0 var(--card-radius) var(--card-radius);
  transform-origin: top;
}

.flip-fold-bottom > span {
  margin-top: calc(var(--card-h) / -2);
}

.flip.flipping .flip-fold-top,
.flip.flipping .flip-fold-bottom {
  display: block;
}

.flip.flipping .flip-fold-top {
  animation: fold-top 150ms ease-in forwards;
}

.flip.flipping .flip-fold-bottom {
  animation: fold-bottom 150ms ease-out 150ms both;
}

@keyframes fold-top {
  from { transform: rotateX(0deg); }
  to   { transform: rotateX(-90deg); }
}

@keyframes fold-bottom {
  from { transform: rotateX(90deg); }
  to   { transform: rotateX(0deg); }
}
```

- [ ] **Step 3: flip-digit.js — 플립 트리거 구현**

`void this.root.offsetWidth`는 강제 reflow다. 없으면 브라우저가 클래스 제거/추가를 한 프레임에 합쳐버려 **애니메이션이 재시작되지 않는다** — 카드가 한 번 넘어간 뒤 멈춘다. 삭제하면 안 된다.

`animationend`를 `foldBottom`에만 건다. `root`에 걸면 150ms 시점의 `fold-top` 종료에도 발화해 하단이 조기에 교체되고 플립이 끊겨 보인다.

```js
/**
 * 플립 시계의 카드 한 장. 자기 값이 바뀔 때만 플립한다.
 *
 * 레이어:
 *   .flip-top         새 값 상단 — 접히는 카드 뒤에서 드러남
 *   .flip-bottom      이전 값 하단 — 펼쳐지는 카드가 덮을 때까지 유지
 *   .flip-fold-top    이전 값 상단 — 아래로 접힘 (0~150ms)
 *   .flip-fold-bottom 새 값 하단 — 위에서 펼쳐짐 (150~300ms)
 */
export class FlipDigit {
  /** @param {HTMLElement} root `[data-flip]` 요소 */
  constructor(root) {
    this.root = root;
    this.topSpan = root.querySelector('.flip-top > span');
    this.bottomSpan = root.querySelector('.flip-bottom > span');
    this.foldTopSpan = root.querySelector('.flip-fold-top > span');
    this.foldBottomSpan = root.querySelector('.flip-fold-bottom > span');
    this.value = null;

    root.querySelector('.flip-fold-bottom').addEventListener('animationend', () => {
      this.bottomSpan.textContent = this.value;
      this.root.classList.remove('flipping');
    });
  }

  /** @param {string} next 한 자리 숫자 문자 */
  set(next) {
    if (next === this.value) return;

    const prev = this.value;
    this.value = next;

    if (prev === null) {
      this.topSpan.textContent = next;
      this.bottomSpan.textContent = next;
      return;
    }

    this.topSpan.textContent = next;
    this.bottomSpan.textContent = prev;
    this.foldTopSpan.textContent = prev;
    this.foldBottomSpan.textContent = next;

    // 애니메이션 재시작을 위한 강제 reflow. 제거하면 카드가 한 번만 넘어간다.
    this.root.classList.remove('flipping');
    void this.root.offsetWidth;
    this.root.classList.add('flipping');
  }
}
```

- [ ] **Step 4: 실행 및 육안 검증**

```bash
cd /mnt/c/Users/cloocus/ms-timer && cmd.exe /c "npx electron ."
```

Expected — 다음을 **전부** 확인한다:
1. 초의 일의자리가 1초마다 **종이가 아래로 넘어가듯** 플립한다
2. **1회로 끝나지 않고 매초 반복된다** — 한 번만 넘어가고 멈추면 Step 3의 강제 reflow가 빠진 것이다
3. 플립 중 상단에 새 숫자, 하단에 이전 숫자가 보이다가 하단이 덮인다
4. 플립이 끝난 뒤 잔상이나 이전 값이 남지 않는다 — 남으면 `display: none` 복귀 문제다
5. 10초를 기다려 **초의 십의자리도 플립**하는지 확인한다 (자릿수별 독립 동작)
6. 카드가 넘어갈 때 좌우로 흔들리거나 레이아웃이 밀리지 않는다

- [ ] **Step 5: 커밋**

```bash
cd /mnt/c/Users/cloocus/ms-timer
git add src/renderer/index.html src/renderer/style.css src/renderer/flip-digit.js
git commit -m "feat: 플립 애니메이션

split-flap 물리 재현 — 이전 값 상단이 접히고(0~150ms)
새 값 하단이 펼쳐진다(150~300ms). transform만 사용해 GPU 합성.
자릿수별 독립 플립 — 각 카드는 자기 값이 바뀔 때만 넘어간다."
```

---

### Task 4: ms 스크롤 릴

플립할 수 없는 ms 3자리. 이 태스크가 "시간이 미친듯이 흐른다"를 만든다.

**Files:**
- Create: `/mnt/c/Users/cloocus/ms-timer/src/renderer/reel.js`
- Modify: `/mnt/c/Users/cloocus/ms-timer/src/renderer/index.html` (릴 3개 추가)
- Modify: `/mnt/c/Users/cloocus/ms-timer/src/renderer/style.css` (릴 스타일 추가)
- Modify: `/mnt/c/Users/cloocus/ms-timer/src/renderer/clock.js` (릴 갱신 추가)

**Interfaces:**
- Consumes: `reelPhase(ms, place)` from `src/lib/reel-phase.js` (Task 1); `#clock` / `.frame` CSS 변수 (Task 2)
- Produces: `Reel` 클래스 — 생성자 `new Reel(rootEl, place: number, blurPx: number)`, 메서드 `update(ms: number)`

**측정 없는 위치 계산:** 스트립은 숫자 11개(`0..9` + wrap용 `0`) 높이이고 릴 창은 1개 높이다. 따라서 `translateY(-phase * 100 / 11 %)`가 정확히 `-phase * 자릿높이`가 된다 — 퍼센트가 스트립 **자신의** 높이 기준이기 때문이다. `clientHeight`를 읽지 않으므로 매 프레임 layout thrash가 없다.

- [ ] **Step 1: reel.js 작성**

블러는 속임수가 아니다. 일의자리는 초당 1000회 변하는데 60회만 샘플링되므로 위치가 프레임마다 튄다 — 블러 없이는 매끄러운 잔상이 아니라 지터로 보인다. 위치는 실제 ms에서 계산되므로 블러는 물리적 사실의 시각화다.

```js
import { reelPhase } from '../lib/reel-phase.js';

const STRIP_DIGITS = 11; // 0..9 + wrap용 0

/**
 * 연속 회전하는 숫자 릴 하나.
 *
 * 플립 애니메이션은 최소 250ms가 필요해 ms 자리(1~100ms 주기)에 쓸 수 없다.
 * 릴은 이산 전환이 아니라 연속 이동이므로 속도 제한이 없다.
 */
export class Reel {
  /**
   * @param {HTMLElement} root `[data-reel]` 요소
   * @param {number} place 100 | 10 | 1
   * @param {number} blurPx 회전 속도에 대응하는 모션 블러 강도
   */
  constructor(root, place, blurPx) {
    this.strip = root.querySelector('.reel-strip');
    this.place = place;
    if (blurPx > 0) {
      this.strip.style.filter = `blur(${blurPx}px)`;
    }
  }

  /** @param {number} ms 0..999 */
  update(ms) {
    const phase = reelPhase(ms, this.place);
    this.strip.style.transform = `translateY(${(-phase * 100) / STRIP_DIGITS}%)`;
  }
}
```

- [ ] **Step 2: index.html — 릴 3개 추가**

`.frame` 안 마지막 `.flip` **뒤**, `</div>` 앞에 삽입한다. 각 스트립은 `0..9` + wrap용 `0` = 11개다.

```html
        <div class="dot">.</div>

        <div class="reel" data-reel="100">
          <div class="reel-strip">
            <span>0</span><span>1</span><span>2</span><span>3</span><span>4</span><span>5</span><span>6</span><span>7</span><span>8</span><span>9</span><span>0</span>
          </div>
        </div>
        <div class="reel" data-reel="10">
          <div class="reel-strip">
            <span>0</span><span>1</span><span>2</span><span>3</span><span>4</span><span>5</span><span>6</span><span>7</span><span>8</span><span>9</span><span>0</span>
          </div>
        </div>
        <div class="reel" data-reel="1">
          <div class="reel-strip">
            <span>0</span><span>1</span><span>2</span><span>3</span><span>4</span><span>5</span><span>6</span><span>7</span><span>8</span><span>9</span><span>0</span>
          </div>
        </div>
```

- [ ] **Step 3: style.css — 릴 스타일 추가**

`.label` 규칙 **앞에** 삽입한다. `--reel-h`는 카드 높이의 40%로, 릴이 카드보다 작아 주인공을 뺏지 않는다.

```css
.dot {
  align-self: flex-end;
  font-size: calc(var(--digit-size) * 0.5);
  font-weight: 600;
  color: var(--sep-fg);
  padding: 0 calc(var(--gap) * 0.5) calc(var(--card-h) * 0.08);
}

.reel {
  position: relative;
  width: calc(var(--card-h) * 0.28);
  height: calc(var(--card-h) * 0.4);
  overflow: hidden;
}

.reel-strip {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  will-change: transform;
}

.reel-strip > span {
  display: block;
  height: calc(var(--card-h) * 0.4);
  line-height: calc(var(--card-h) * 0.4);
  text-align: center;
  font-size: calc(var(--card-h) * 0.32);
  font-weight: 600;
  color: var(--reel-fg);
}

.clock.expired .reel-strip > span {
  color: var(--accent);
}
```

- [ ] **Step 4: clock.js — 릴 갱신 추가**

전체 파일을 아래로 교체한다. 릴은 카드와 달리 값 비교를 하지 않는다 — 연속 회전이므로 매 프레임 위치가 실제로 다르다.

블러 강도는 자릿수 회전 속도에 대응한다: 백의자리 1바퀴/초(선명), 십의자리 10바퀴/초(번짐), 일의자리 100바퀴/초(블러).

```js
import { computeRemaining } from '../lib/countdown.js';
import { FlipDigit } from './flip-digit.js';
import { Reel } from './reel.js';

const clockEl = document.getElementById('clock');
const labelEl = document.getElementById('label');
const digits = [...document.querySelectorAll('[data-flip]')].map((el) => new FlipDigit(el));

const reels = [
  new Reel(document.querySelector('[data-reel="100"]'), 100, 0),
  new Reel(document.querySelector('[data-reel="10"]'), 10, 1),
  new Reel(document.querySelector('[data-reel="1"]'), 1, 3),
];

const pad2 = (n) => String(n).padStart(2, '0');

let lastExpired = null;

function tick() {
  const { expired, h, m, s, ms } = computeRemaining(new Date());

  const chars = pad2(h) + pad2(m) + pad2(s);
  for (let i = 0; i < digits.length; i++) {
    digits[i].set(chars[i]);
  }

  for (const reel of reels) {
    reel.update(ms);
  }

  if (expired !== lastExpired) {
    clockEl.classList.toggle('expired', expired);
    labelEl.textContent = expired ? '퇴근' : '퇴근까지';
    lastExpired = expired;
  }

  requestAnimationFrame(tick);
}

requestAnimationFrame(tick);
```

- [ ] **Step 5: 실행 및 육안 검증**

```bash
cd /mnt/c/Users/cloocus/ms-timer && cmd.exe /c "npx electron ."
```

Expected — 다음을 **전부** 확인한다:
1. 카드 오른쪽에 릴 3개가 돌아간다
2. **왼쪽 릴은 읽힐 듯 말 듯, 가운데는 번지고, 오른쪽은 거의 순수 블러다** — 이 대비가 이 태스크의 목적이다
3. 릴에 **빈칸이 지나가지 않는다** — 보이면 wrap용 11번째 숫자나 `translateY` 계산 문제다
4. 릴이 위아래로 튀지 않고 한 방향(아래)으로 흐른다
5. 플립 카드와 릴이 동시에 움직여도 프레임이 끊기지 않는다
6. 창을 리사이즈해도 릴이 카드와 비율을 유지한다

- [ ] **Step 6: expired 상태 검증**

18시 도달 동작은 spec의 핵심이지만 실제 18시를 기다릴 수 없다. 시스템 시계를 건드리지 않고 검증하기 위해 목표 시각을 이미 지난 시각으로 임시 변경한다.

`src/renderer/clock.js`의 `computeRemaining(new Date())`를 임시로 아래처럼 고친다(목표를 새벽 1시로 → 이미 지남):

```js
  const { expired, h, m, s, ms } = computeRemaining(new Date(), 1);
```

실행:
```bash
cd /mnt/c/Users/cloocus/ms-timer && cmd.exe /c "npx electron ."
```

Expected — 다음을 **전부** 확인한다:
1. 카드 6장 전부 `00:00:00` 표시
2. 릴 3개가 전부 `0`에서 **정지** (ms=0이므로 위상 0)
3. 숫자와 릴이 accent 색(초록)으로 바뀐다
4. 라벨이 "퇴근"으로 바뀐다

**확인 후 반드시 되돌린다:**

```js
  const { expired, h, m, s, ms } = computeRemaining(new Date());
```

되돌린 뒤 다시 실행해 카운트다운이 정상 복귀했는지 재확인한다. 이 복구를 빠뜨리면 Task 6의 exe가 영구히 "퇴근"만 표시한다.

- [ ] **Step 7: 커밋**

`git diff`로 Step 6의 임시 변경이 남아 있지 않은지 먼저 확인한다.

```bash
cd /mnt/c/Users/cloocus/ms-timer
git diff src/renderer/clock.js | grep -n "new Date(), 1" && echo "!! Step 6 임시 변경이 남아 있다 — 되돌릴 것" || echo "OK — 임시 변경 없음"
git add src/renderer/reel.js src/renderer/index.html src/renderer/style.css src/renderer/clock.js
git commit -m "feat: ms 스크롤 릴 3개

플립 불가 자리(1~100ms 주기)의 표현. 연속 위상 이동이라 속도 제한 없음.
자리별 블러(0/1/3px)로 60Hz 샘플링 지터를 모션 블러로 지각시킨다.
스트립 11자리 + translateY 퍼센트 — clientHeight 측정 없이 wrap."
```

---

### Task 5: 테마 2개 + T 키 토글

**Files:**
- Create: `/mnt/c/Users/cloocus/ms-timer/src/renderer/theme.js`
- Modify: `/mnt/c/Users/cloocus/ms-timer/src/renderer/clock.js` (initTheme 호출 1줄)

**Interfaces:**
- Consumes: `themes.css`의 `:root[data-theme="dark"]` / `:root[data-theme="retro"]` 팔레트 (Task 2 Step 5에서 이미 정의됨)
- Produces: `initTheme()` — 인자 없음, 반환 없음. `clock.js`가 rAF 시작 전에 1회 호출한다.

**localStorage 가정 처리:** Electron이 `file://` origin에 localStorage를 허용하는지는 미검증이다. `try/catch`로 감싸면 **양쪽 세계에서 모두 옳다** — 허용되면 영속화되고, 막히면 조용히 세션 전용으로 떨어진다. spec의 fallback이 코드에 내장된 것이지 "해보고 안 되면 고친다"가 아니다. Step 4에서 실제로 어느 쪽인지 확인해 README에 기록한다.

- [ ] **Step 1: theme.js 작성**

`THEMES.includes(saved)` 검사는 localStorage가 오염됐을 때(수동 편집, 구버전 값) 정의되지 않은 테마로 부팅되는 것을 막는다.

```js
const THEMES = ['dark', 'retro'];
const STORAGE_KEY = 'ms-timer:theme';

/**
 * localStorage는 Electron의 file:// origin에서 막힐 수 있다.
 * 막히면 세션 전용 토글로 동작한다 — 기능은 유지된다.
 */
function readTheme() {
  try {
    const saved = localStorage.getItem(STORAGE_KEY);
    return THEMES.includes(saved) ? saved : THEMES[0];
  } catch {
    return THEMES[0];
  }
}

function writeTheme(theme) {
  try {
    localStorage.setItem(STORAGE_KEY, theme);
  } catch {
    // 영속화 불가 — 세션 전용으로 동작한다
  }
}

/** T 키로 테마를 순환시킨다. rAF 루프 시작 전에 1회 호출한다. */
export function initTheme() {
  let current = readTheme();
  document.documentElement.dataset.theme = current;

  window.addEventListener('keydown', (e) => {
    if (e.key !== 't' && e.key !== 'T') return;
    current = THEMES[(THEMES.indexOf(current) + 1) % THEMES.length];
    document.documentElement.dataset.theme = current;
    writeTheme(current);
  });
}
```

- [ ] **Step 2: clock.js — initTheme 배선**

import 블록 마지막에 추가:

```js
import { initTheme } from './theme.js';
```

파일 맨 아래 `requestAnimationFrame(tick);`를 아래로 교체:

```js
initTheme();
requestAnimationFrame(tick);
```

- [ ] **Step 3: main.js — 다크 테마 배경색 유지 확인**

`backgroundColor: '#0b0d10'`은 다크 테마 값이다. 레트로로 저장된 상태에서 재실행하면 로드 순간 어두운 배경이 한 프레임 스쳤다가 민트로 바뀐다. 창 생성 시점에 renderer의 저장값을 알 수 없으므로(IPC 없이는) 이는 수용한다 — 한 프레임 깜빡임 때문에 프로세스 간 통신 계층을 추가할 가치가 없다.

변경 없음. 이 스텝은 의도적 수용을 기록하기 위한 것이다.

- [ ] **Step 4: 실행 및 검증 — localStorage 동작 여부 확정**

```bash
cd /mnt/c/Users/cloocus/ms-timer && cmd.exe /c "npx electron ."
```

Expected — 다음을 **전부** 확인한다:
1. 다크 모던 테마로 뜬다 (검은 배경 / 흰 숫자)
2. **`T` 키를 누르면** 레트로 민트로 바뀐다 (민트 프레임 / 크림 카드 / 딥그린 숫자)
3. `T`를 다시 누르면 다크로 돌아온다
4. 테마가 바뀌어도 플립·릴이 멈추지 않는다
5. 레트로 테마에서 크림 카드 위 딥그린 숫자가 충분히 읽힌다

**localStorage 확정:** 레트로 상태로 두고 창을 닫은 뒤 재실행한다.
- 레트로로 뜨면 → localStorage 동작. Step 5에서 README에 "테마가 기억된다"로 기록한다.
- 다크로 뜨면 → `file://`에서 막힘. Step 5에서 README에 "테마는 세션 전용"으로 기록한다. **코드는 고치지 않는다** — try/catch가 이미 올바르게 처리한 결과다.

- [ ] **Step 5: 커밋**

Step 4의 확정 결과를 커밋 메시지 마지막 줄에 반영한다(둘 중 사실인 쪽만 남긴다).

```bash
cd /mnt/c/Users/cloocus/ms-timer
git add src/renderer/theme.js src/renderer/clock.js
git commit -m "feat: 다크/레트로 테마 + T 키 토글

팔레트는 CSS 변수로만 분리 — style.css의 구조와 침범하지 않는다.
localStorage는 try/catch — file:// origin에서 막히면 세션 전용으로 동작.
실측 결과: 영속화 <동작함 | 막힘 — 세션 전용>"
```

---

### Task 6: portable exe 빌드

**Files:**
- Create: `/mnt/c/Users/cloocus/ms-timer/README.md`

**Interfaces:**
- Consumes: Task 1의 `package.json` `build` 설정, Task 2의 `src/main.js` 진입점
- Produces: `dist/ms-timer.exe` — 설치 불필요 단일 실행 파일

- [ ] **Step 1: 전체 테스트 재확인**

빌드 전 게이트. 여기서 실패하면 빌드하지 않는다.

```bash
cd /mnt/c/Users/cloocus/ms-timer && node --test
```

Expected: `# pass 17`, `# fail 0`

- [ ] **Step 2: 빌드 실행**

첫 빌드는 electron-builder가 캐시를 받으므로 2-5분 걸린다.

```bash
cd /mnt/c/Users/cloocus/ms-timer && cmd.exe /c "npx electron-builder --win portable"
```

Expected: `building target=portable file=dist\ms-timer.exe` 후 종료 코드 0

- [ ] **Step 3: 산출물 확인**

```bash
ls -lh /mnt/c/Users/cloocus/ms-timer/dist/ms-timer.exe
```

Expected: `ms-timer.exe`, 60-100MB 대

- [ ] **Step 4: 포장된 exe로 실행 검증**

**개발 모드가 아닌 exe로** 확인한다. asar 패키징 후 경로가 깨지는 일이 흔하므로 이 단계를 건너뛰면 안 된다.

```bash
cd /mnt/c/Users/cloocus/ms-timer/dist && cmd.exe /c "start ms-timer.exe"
```

Expected — 다음을 **전부** 확인한다:
1. 창이 뜨고 카드가 플립하며 릴이 돈다 (개발 모드와 동일)
2. `T` 키 테마 토글이 동작한다
3. 흰 화면이나 "Cannot find module"이 뜨지 않는다 — 뜨면 `package.json`의 `build.files`에 `src/**/*`가 있는지 확인한다

- [ ] **Step 5: README 작성**

Task 5 Step 4에서 확정한 localStorage 결과를 "테마" 절에 반영한다(둘 중 사실인 쪽만 남긴다).

````markdown
# ms-timer

18시 정각까지 남은 시간을 1ms 단위로 표시하는 플립 시계.

시:분:초는 플립 카드가 착착 넘어가고, ms 3자리는 읽을 수 없는 속도로 흐른다.

## 실행

`dist/ms-timer.exe` — 설치 불필요.

- **`T`** — 테마 전환 (다크 모던 ↔ 레트로 민트)

18시에 도달하면 `00:00:00.000`에서 멈추고 "퇴근"을 표시한다.
자정을 넘기면 자동으로 다음 날 18시 카운트다운이 시작되므로 켜둔 채로 둬도 된다.

테마는 <다음 실행 시 기억된다 | 세션 전용이며 매번 다크로 시작한다>.

## 왜 ms 자리는 플립하지 않나

플립 애니메이션 1회는 최소 250ms가 필요하다. 그보다 짧으면 종이가 넘어가는 것으로
지각되지 않고 깜빡임이 된다. ms 자리는 1~100ms마다 바뀌므로 구조적으로 플립할 수 없다.

그래서 릴로 흐르게 했다. 모니터는 60Hz(16.7ms)이므로 오른쪽 릴은 초당 100바퀴를
돌지만 60번만 샘플링된다 — 읽을 수 없는 것이 정상이고 의도다.
"시간이 흐른다"(플립)와 "시간이 미친듯이 흐른다"(릴)를 한 화면에서 대비시키는 것이
이 앱의 전부다.

## 개발

프로젝트는 반드시 `/mnt/c` 아래에 둔다. WSL 파일시스템에 두면 Windows node가
`\\wsl$` UNC 경로로 접근하며 electron-builder가 실패한다.

```bash
node --test                              # 테스트 (WSL node)
cmd.exe /c "npm install"                 # 의존성 (Windows node 필수)
cmd.exe /c "npx electron ."              # 개발 실행
cmd.exe /c "npx electron-builder --win portable"   # exe 빌드 → dist/ms-timer.exe
```

`npm install`을 WSL node로 하면 linux용 electron 바이너리가 받아져 실행되지 않는다.

## 구조

```
src/lib/          순수 함수 — 로직 전부, 테스트 전부, DOM 모름
  countdown.js      시간 계산
  reel-phase.js     릴 위상 계산
src/renderer/     표현 계층 — 로직 없음, 테스트 없음
  flip-digit.js     카드 1장
  reel.js           릴 1개
  theme.js          테마 토글
  clock.js          조립 + rAF 루프
src/main.js       BrowserWindow 생성
```

시간 계산은 절대 시각 차분(`오늘 18시 - now`)이다. 누적 감산이 아니므로
드리프트가 없고 절전 복귀·NTP 보정·타임존 변경이 자동으로 반영된다.

그래픽이 통째로 바뀌어도 `src/lib/`는 손대지 않는다 — 이 경계가 요점이다.
````

- [ ] **Step 6: 커밋**

```bash
cd /mnt/c/Users/cloocus/ms-timer
git add README.md
git commit -m "docs: README — 실행/빌드/구조

WSL·Windows node 분리 제약과 ms를 플립하지 않는 이유를 기록."
```

---

## 완료 기준

- [ ] `node --test` — 17개 통과, 0개 실패
- [ ] `dist/ms-timer.exe` 생성됨
- [ ] exe 실행 → 초 카드가 매초 플립하고 릴 3개가 돈다
- [ ] 릴이 왼쪽에서 오른쪽으로 갈수록 흐려진다
- [ ] `T` 키로 테마가 전환된다
- [ ] 카드/릴이 움직여도 레이아웃이 흔들리지 않는다
- [ ] 창을 닫으면 프로세스 종료
- [ ] `src/renderer/clock.js`에 Task 4 Step 6의 임시 `targetHour` 변경이 남아 있지 않다
